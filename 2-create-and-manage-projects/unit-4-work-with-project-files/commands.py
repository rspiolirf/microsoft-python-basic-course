# creates a list of packages that the project needs
# pip freeze > requirements.txt

# install the packages needed for the project
# pip install -r requirements.txt

# upgrading packages with a specific version
# pip install python-dateutil==1.4

# upgrade to lastest version
# pip install python-dateutil --upgrade

# upgrade just minor version
# pip install "python-dateutil==2.7.*" --upgrade

# uninstall package
# pip uninstall python-dateutil